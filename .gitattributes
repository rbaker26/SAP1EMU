# The reason this is here is because GitHub thinks that the binary test files for the emulator are
#  written in BrainFuck and will display BrainFuck on the Repo's main page.
# To avoid the word "Fuck" on my main repo page, I had to put this here.
# Thanks GitHub
*.b -linguist-detectable


# this is to make github not count the Assembly test files as a part of the code
*.s -linguist-detectable 
